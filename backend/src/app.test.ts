import { describe, it, expect } from 'vitest';

describe('ControlTaco Backend', () => {
  it('el entorno de pruebas funciona correctamente', () => {
    expect(true).toBe(true);
  });

  it('operaciones matemáticas básicas', () => {
    expect(2 + 2).toBe(4);
  });
});